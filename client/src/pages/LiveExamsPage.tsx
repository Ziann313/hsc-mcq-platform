import { useAuth } from "@/_core/hooks/useAuth";
import { PlatformShell, type Language } from "@/components/PlatformShell";
import { AdminLiveRoomManagement } from "@/components/AdminLiveRoomManagement";
import { AdminChallengeDataStatus } from "@/components/AdminChallengeDataStatus";
import { DailyChallengeAlertPreference } from "@/components/DailyChallengeAlertPreference";
import { DailyChallengeAlertSummary } from "@/components/DailyChallengeAlertSummary";
import { LiveResultReview } from "@/components/LiveResultReview";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { AlertTriangle, CalendarClock, CheckCircle2, ChevronLeft, Clock3, Flame, Loader2, ShieldAlert, Trophy, Users } from "lucide-react";
import { useEffect, useRef, useState } from "react";
import { toast } from "sonner";
import { useLocation, useRoute } from "wouter";

type Props = { language: Language; onLanguageChange: (value: Language) => void };
type LiveQuestion = { id: number; prompt: string; questionType: string; subject: string; chapter: string | null; stemContext: string | null; options: Array<{ id: number; optionKey: string; text: string }> };
type LiveAttempt = { attemptId: number; expiresAt: Date | string; selections: Array<{ questionId: number; selectedOptionIds: number[] }>; questions: LiveQuestion[] };
const copyFor = (language: Language) => (en: string, bn: string) => language === "bn" ? bn : en;

export default function LiveExamsPage({ language, onLanguageChange }: Props) {
  const [, params] = useRoute("/live-exams/:roomId");
  return <PlatformShell language={language} onLanguageChange={onLanguageChange}>{params?.roomId ? <LiveRoom roomId={Number(params.roomId)} language={language} /> : <LiveLobby language={language} />}</PlatformShell>;
}

function LiveLobby({ language }: { language: Language }) {
  const copy = copyFor(language);
  const [, navigate] = useLocation();
  const { user } = useAuth();
  const rooms = trpc.liveExam.list.useQuery(undefined, { refetchInterval: 5_000, refetchOnWindowFocus: true });
  return <main className="mx-auto max-w-6xl space-y-6 pb-[calc(6rem+env(safe-area-inset-bottom))] sm:pb-8">
    <section className="relative overflow-hidden rounded-[28px] bg-[#071d33] p-6 text-white sm:p-8"><div className="absolute -right-16 -top-16 size-72 rounded-full bg-[#16b89b]/20 blur-3xl" /><div className="relative grid gap-5 lg:grid-cols-[1fr_auto] lg:items-end"><div><span className="inline-flex items-center gap-2 rounded-full bg-white/10 px-3 py-1 text-xs font-bold text-[#7ce3d1]"><Flame size={14} />{copy("LIVE MCQ SESSIONS", "লাইভ এমসিকিউ সেশন")}</span><h1 className="mt-4 font-display text-3xl font-extrabold sm:text-4xl">{copy("Compete live. Learn faster.", "লাইভ প্রতিযোগিতা, দ্রুত শেখা।")}</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-300">{copy("Join scheduled public rooms and daily sprints. The shared clock, saved answers, ranking, and result come from the server.", "শিডিউলড পাবলিক রুম ও ডেইলি স্প্রিন্টে যোগ দাও। শেয়ার্ড টাইমার, সেভ হওয়া উত্তর, র‍্যাঙ্ক ও ফলাফল সার্ভার থেকে আসে।")}</p></div><p className="rounded-2xl border border-white/10 bg-white/5 px-5 py-4 text-xs font-bold text-[#7ce3d1]">{copy("Refreshing every 5 seconds", "প্রতি ৫ সেকেন্ডে রিফ্রেশ হচ্ছে")}</p></div></section>
    {user && <DailyChallengeAlertPreference language={language} />}
    {user?.role === "admin" && <><DailyChallengeAlertSummary language={language} /><AdminChallengeDataStatus language={language} /><LiveAdminPanel language={language} /><AdminLiveRoomManagement language={language} /></>}
    <section><div className="mb-4 flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[.15em] text-[#088a78]">{copy("UPCOMING & LIVE", "আসন্ন ও লাইভ")}</p><h2 className="mt-1 font-display text-2xl font-extrabold text-[#071d33]">{copy("Choose your room", "তোমার রুম বেছে নাও")}</h2></div><span className="rounded-xl bg-white px-3 py-2 text-xs font-bold text-slate-500">{rooms.isLoading ? "…" : rooms.data?.length ?? 0} {copy("rooms", "রুম")}</span></div>{rooms.isLoading ? <RoomSkeletons /> : rooms.error ? <RecoveryCard language={language} message={rooms.error.message} onRetry={() => rooms.refetch()} /> : rooms.data?.length ? <div className="grid gap-4 lg:grid-cols-2">{rooms.data.map(room => <article key={room.id} className="rounded-[24px] bg-white p-5 shadow-sm ring-1 ring-slate-100"><div className="flex items-start justify-between gap-3"><div className="min-w-0"><div className="flex flex-wrap gap-2"><StatusPill language={language} status={room.status} />{room.mode === "daily_challenge" && <span className="rounded-full bg-[#fff5e4] px-2.5 py-1 text-[10px] font-bold text-[#b86b09]">{copy("DAILY SPRINT", "ডেইলি স্প্রিন্ট")}</span>}</div><h3 className="mt-3 line-clamp-2 font-display text-lg font-extrabold text-[#071d33]">{room.title}</h3><p className="mt-1 text-xs text-slate-500">{room.description || copy("Source-linked live MCQ room", "সোর্স-লিংকড লাইভ এমসিকিউ রুম")}</p></div><Trophy className="shrink-0 text-[#e5a523]" size={22} /></div><div className="mt-5 grid grid-cols-3 gap-2 border-t border-slate-100 pt-4 text-xs text-slate-500"><Info icon={CalendarClock} label={new Date(room.startsAt).toLocaleString(language === "bn" ? "bn-BD" : "en-GB", { dateStyle: "medium", timeStyle: "short" })} /><Info icon={Clock3} label={`${room.durationMinutes} ${copy("min", "মিনিট")}`} /><Info icon={Users} label={`${room.participantCount} ${copy("joined", "যোগ দিয়েছে")}`} /></div><Button onClick={() => navigate(`/live-exams/${room.id}`)} className="mt-5 min-h-11 w-full rounded-xl bg-[#071d33]">{room.myStatus === "submitted" || room.myStatus === "expired" ? copy("View result & rank", "ফলাফল ও র‍্যাঙ্ক দেখো") : room.status === "live" ? copy("Join live room", "লাইভ রুমে যোগ দাও") : copy("View room", "রুম দেখো")}</Button></article>)}</div> : <EmptyLiveState language={language} />}</section>
  </main>;
}

function LiveAdminPanel({ language }: { language: Language }) {
  const copy = copyFor(language);
  const utils = trpc.useUtils();
  const questions = trpc.mcq.publishedQuestions.useQuery({ limit: 100 });
  const schedules = trpc.liveExam.dailyChallengeSchedules.useQuery();
  const [title, setTitle] = useState(""); const [startsAt, setStartsAt] = useState(""); const [duration, setDuration] = useState(15); const [dailyTimeDhaka, setDailyTimeDhaka] = useState("18:00"); const [selectedIds, setSelectedIds] = useState<number[]>([]);
  const payload = { title: title || copy("Daily MCQ Sprint", "ডেইলি এমসিকিউ স্প্রিন্ট"), durationMinutes: duration, questionIds: selectedIds, marksPerCorrect: 1, negativeMarkPerWrong: 0.25, autoSubmitAfterWarnings: 3 };
  const refresh = () => { utils.liveExam.list.invalidate(); utils.liveExam.dailyChallengeSchedules.invalidate(); };
  const schedule = trpc.liveExam.create.useMutation({ onSuccess: () => { toast.success(copy("Live room scheduled", "লাইভ রুম শিডিউল করা হয়েছে")); refresh(); }, onError: error => toast.error(error.message) });
  const recurring = trpc.liveExam.createDailyChallengeSchedule.useMutation({ onSuccess: () => { toast.success(copy("Daily challenge schedule saved", "ডেইলি চ্যালেঞ্জ শিডিউল সেভ হয়েছে")); refresh(); }, onError: error => toast.error(error.message) });
  const toggle = trpc.liveExam.setDailyChallengeScheduleEnabled.useMutation({ onSuccess: () => { toast.success(copy("Daily challenge schedule updated", "ডেইলি চ্যালেঞ্জ শিডিউল আপডেট হয়েছে")); refresh(); }, onError: error => toast.error(error.message) });
  const toggleQuestion = (id: number) => setSelectedIds(current => current.includes(id) ? current.filter(value => value !== id) : [...current, id]);
  return <section className="rounded-[24px] border border-[#bdeadd] bg-[#effcf9] p-5 sm:p-6"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-start"><div><p className="text-xs font-bold uppercase tracking-[.15em] text-[#087b6c]">{copy("ADMIN LIVE CONTROL", "অ্যাডমিন লাইভ কন্ট্রোল")}</p><h2 className="mt-1 font-display text-xl font-extrabold text-[#071d33]">{copy("Schedule source-validated challenges", "সোর্স-ভ্যালিডেটেড চ্যালেঞ্জ শিডিউল করো")}</h2><p className="mt-2 max-w-2xl text-xs leading-5 text-[#386e66]">{copy("Recurring challenges run daily at the chosen Bangladesh time and only use your published, source-linked questions.", "রিকারিং চ্যালেঞ্জ বেছে নেওয়া বাংলাদেশ সময় অনুযায়ী প্রতিদিন চলে এবং শুধু প্রকাশিত, সোর্স-লিংকড প্রশ্ন ব্যবহার করে।")}</p></div><span className="rounded-xl bg-white px-3 py-2 text-xs font-bold text-[#087b6c]">{questions.data?.length ?? 0} {copy("eligible questions", "যোগ্য প্রশ্ন")}</span></div>
    <div className="mt-5 grid gap-3 lg:grid-cols-4"><input value={title} onChange={event => setTitle(event.target.value)} placeholder={copy("Challenge title", "চ্যালেঞ্জের নাম")} className="min-h-11 rounded-xl border border-[#bdeadd] bg-white px-3 text-sm" /><input type="datetime-local" value={startsAt} onChange={event => setStartsAt(event.target.value)} className="min-h-11 rounded-xl border border-[#bdeadd] bg-white px-3 text-sm" /><select value={duration} onChange={event => setDuration(Number(event.target.value))} className="min-h-11 rounded-xl border border-[#bdeadd] bg-white px-3 text-sm"><option value={10}>10 min</option><option value={15}>15 min</option><option value={30}>30 min</option></select><Button disabled={schedule.isPending || !startsAt || !selectedIds.length} onClick={() => schedule.mutate({ ...payload, mode: "scheduled", startsAt: new Date(startsAt) })} className="min-h-11 rounded-xl bg-[#071d33]">{copy("Schedule once", "একবার শিডিউল")}</Button></div>
    <div className="mt-3 grid gap-3 rounded-2xl border border-[#bdeadd] bg-white/70 p-4 sm:grid-cols-[1fr_auto_auto]"><div><p className="text-xs font-bold text-[#087b6c]">{copy("RECURRING DAILY CHALLENGE", "রিকারিং ডেইলি চ্যালেঞ্জ")}</p><p className="mt-1 text-xs text-slate-500">{copy("Uses Bangladesh time; you can pause it anytime.", "বাংলাদেশ সময় ব্যবহার করে; যেকোনো সময় বিরতি দেওয়া যায়।")}</p></div><label className="flex min-h-11 items-center gap-2 rounded-xl border border-[#bdeadd] bg-white px-3 text-sm"><span className="text-xs font-bold text-slate-500">{copy("Time", "সময়")}</span><input type="time" value={dailyTimeDhaka} onChange={event => setDailyTimeDhaka(event.target.value)} className="w-24 border-0 bg-transparent p-0" /></label><Button disabled={recurring.isPending || !selectedIds.length} onClick={() => recurring.mutate({ ...payload, dailyTimeDhaka })} className="min-h-11 rounded-xl bg-[#087b6c]">{recurring.isPending ? <Loader2 className="animate-spin" size={16} /> : <Flame size={16} />}{copy("Schedule daily", "প্রতিদিন শিডিউল")}</Button></div>
    <div className="mt-4 max-h-44 overflow-y-auto rounded-xl bg-white p-3"><p className="text-xs font-bold text-slate-500">{copy("Select published source-linked questions", "প্রকাশিত সোর্স-লিংকড প্রশ্ন বেছে নাও")}</p>{questions.isLoading ? <div className="mt-3 h-10 animate-pulse rounded-lg bg-slate-100" /> : questions.data?.length ? <div className="mt-3 space-y-2">{questions.data.map(question => <label key={question.id} className="flex min-h-11 cursor-pointer items-center gap-3 rounded-lg px-2 text-xs hover:bg-slate-50"><input type="checkbox" checked={selectedIds.includes(question.id)} onChange={() => toggleQuestion(question.id)} /><span className="line-clamp-1">{question.prompt}</span></label>)}</div> : <p className="mt-3 text-xs text-slate-500">{copy("No published source-linked questions are available yet.", "এখনও কোনো প্রকাশিত সোর্স-লিংকড প্রশ্ন নেই।")}</p>}</div>
    <div className="mt-5 border-t border-[#bdeadd] pt-4"><p className="text-xs font-bold uppercase tracking-[.14em] text-[#087b6c]">{copy("DAILY CHALLENGE SCHEDULES", "ডেইলি চ্যালেঞ্জ শিডিউল")}</p>{schedules.isLoading ? <div className="mt-3 h-14 animate-pulse rounded-xl bg-white/70" /> : schedules.data?.length ? <div className="mt-3 space-y-2">{schedules.data.map(item => <div key={item.id} className="flex flex-wrap items-center gap-2 rounded-xl bg-white p-3 text-xs"><span className={`rounded-full px-2 py-1 font-bold ${item.isEnabled ? "bg-[#e9fbf7] text-[#087b6c]" : "bg-slate-100 text-slate-500"}`}>{item.isEnabled ? copy("ACTIVE", "চালু") : copy("PAUSED", "বিরত")}</span><span className="min-w-28 flex-1 font-bold text-[#071d33]">{item.title}</span><span className="text-slate-500">{item.cronExpression}</span><Button variant="outline" disabled={toggle.isPending || !item.scheduleCronTaskUid} onClick={() => toggle.mutate({ scheduleId: item.id, isEnabled: !item.isEnabled })} className="min-h-9 rounded-lg px-3">{item.isEnabled ? copy("Pause", "বিরতি") : copy("Resume", "চালু")}</Button></div>)}</div> : <p className="mt-3 text-xs text-slate-500">{copy("No recurring challenge has been scheduled yet.", "এখনও কোনো রিকারিং চ্যালেঞ্জ শিডিউল করা হয়নি।")}</p>}</div>
  </section>;
}

function LiveRoom({ roomId, language }: { roomId: number; language: Language }) {
  const copy = copyFor(language); const [, navigate] = useLocation();
  const detail = trpc.liveExam.detail.useQuery({ roomId }, { refetchInterval: 5_000, refetchOnWindowFocus: true });
  const join = trpc.liveExam.join.useMutation({ onError: error => toast.error(error.message) }); const submit = trpc.liveExam.submit.useMutation({ onError: error => toast.error(error.message) }); const save = trpc.mcq.saveAttemptSelection.useMutation({ onError: error => toast.error(error.message) });
  const integrity = trpc.liveExam.reportIntegrity.useMutation({ onError: () => toast.warning(copy("Connection warning could not be saved. Keep this tab open.", "কানেকশন ওয়ার্নিং সেভ করা যায়নি। এই ট্যাব খোলা রাখো।")) });
  const [attempt, setAttempt] = useState<LiveAttempt | null>(null); const [selected, setSelected] = useState<Record<number, number>>({}); const [activeIndex, setActiveIndex] = useState(0); const [remaining, setRemaining] = useState(0); const submitted = useRef(false);
  const finish = () => { if (!attempt || submitted.current || submit.isPending) return; submitted.current = true; submit.mutate({ roomId, selections: Object.entries(selected).map(([questionId, optionId]) => ({ questionId: Number(questionId), selectedOptionIds: [optionId] })) }, { onSuccess: () => { toast.success(copy("Live exam submitted", "লাইভ এক্সাম জমা হয়েছে")); detail.refetch(); navigate(`/live-exams/${roomId}`); } }); };
  useEffect(() => { if (!attempt) return; const tick = () => { const seconds = Math.max(0, Math.ceil((new Date(attempt.expiresAt).getTime() - Date.now()) / 1000)); setRemaining(seconds); if (seconds === 0) finish(); }; tick(); const timer = window.setInterval(tick, 1000); return () => window.clearInterval(timer); }, [attempt, selected]);
  useEffect(() => { if (!attempt) return; const onVisibility = () => { if (document.visibilityState === "hidden") integrity.mutate({ roomId, eventType: "visibility_hidden", metadata: { at: new Date().toISOString() } }, { onSuccess: result => { toast.warning(result.autoSubmitted ? copy("Repeated tab changes submitted your live exam.", "বারবার ট্যাব পরিবর্তনের কারণে লাইভ এক্সাম জমা হয়েছে।") : copy(`Focus warning ${result.warningCount}`, `ফোকাস ওয়ার্নিং ${result.warningCount}`)); if (result.autoSubmitted) { submitted.current = true; detail.refetch(); } } }); }; document.addEventListener("visibilitychange", onVisibility); return () => document.removeEventListener("visibilitychange", onVisibility); }, [attempt, roomId]);
  if (detail.isLoading) return <LiveRoomSkeleton />; if (detail.error || !detail.data) return <RecoveryCard language={language} message={detail.error?.message ?? copy("Live room not found", "লাইভ রুম পাওয়া যায়নি")} onRetry={() => detail.refetch()} />;
  const { room, participantCount, participant, leaderboard } = detail.data; const isComplete = participant?.status === "submitted" || participant?.status === "expired";
  const start = () => join.mutate({ roomId }, { onSuccess: data => { const active = data as LiveAttempt; setAttempt(active); setSelected(Object.fromEntries(active.selections.flatMap(item => item.selectedOptionIds[0] ? [[item.questionId, item.selectedOptionIds[0]]] : []))); setActiveIndex(0); submitted.current = false; toast.success(active.selections.length ? copy("Saved answers restored", "সেভ করা উত্তর ফিরিয়ে আনা হয়েছে") : copy("You joined the live room", "তুমি লাইভ রুমে যোগ দিয়েছো")); } });
  if (!attempt) return <main className="mx-auto max-w-5xl space-y-5 pb-[calc(6rem+env(safe-area-inset-bottom))]"><button onClick={() => navigate("/live-exams")} className="inline-flex min-h-11 items-center gap-2 text-sm font-bold text-[#087b6c]"><ChevronLeft size={17} />{copy("All live exams", "সব লাইভ এক্সাম")}</button><section className="rounded-[28px] bg-[#071d33] p-6 text-white sm:p-8"><div className="flex flex-col justify-between gap-5 sm:flex-row"><div><StatusPill status={room.status} language={language} /><h1 className="mt-4 font-display text-3xl font-extrabold">{room.title}</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-300">{room.description || copy("A shared-clock source-linked MCQ session.", "শেয়ার্ড ক্লক সোর্স-লিংকড এমসিকিউ সেশন।")}</p></div><div className="grid grid-cols-2 gap-3 rounded-2xl border border-white/10 bg-white/5 p-4 text-xs"><Info icon={Users} label={`${participantCount} ${copy("participants", "পার্টিসিপেন্ট")}`} dark /><Info icon={Clock3} label={`${room.durationMinutes} ${copy("min", "মিনিট")}`} dark /></div></div><div className="mt-6">{room.status === "live" && !isComplete ? <Button onClick={start} disabled={join.isPending} className="min-h-12 rounded-xl bg-[#16b89b] text-[#062033] hover:bg-[#24c8aa]">{join.isPending ? <Loader2 className="animate-spin" size={17} /> : <Flame size={17} />}{participant?.attemptId ? copy("Resume live exam", "লাইভ এক্সাম চালিয়ে যাও") : copy("Join live exam", "লাইভ এক্সামে যোগ দাও")}</Button> : <p className="rounded-xl bg-white/10 px-4 py-3 text-sm font-semibold">{room.status === "scheduled" ? copy("The room opens at its scheduled time.", "নির্ধারিত সময়ে রুম চালু হবে।") : copy("This room is closed. See your result and final ranking below.", "রুমটি বন্ধ। নিচে তোমার ফলাফল ও চূড়ান্ত র‍্যাঙ্কিং দেখো।")}</p>}<p className="mt-3 flex items-center gap-2 text-xs text-slate-300"><ShieldAlert size={15} className="text-[#7ce3d1]" />{copy("Repeated tab changes are logged.", "বারবার ট্যাব পরিবর্তন লগ করা হয়।")}</p></div></section>{isComplete ? <><LiveResultReview roomId={roomId} language={language} /><Leaderboard language={language} rows={leaderboard} /></> : <Leaderboard language={language} rows={leaderboard} />}</main>;
  const question = attempt.questions[activeIndex]; const time = `${String(Math.floor(remaining / 60)).padStart(2, "0")}:${String(remaining % 60).padStart(2, "0")}`;
  return <main className="mx-auto max-w-6xl pb-[calc(6.5rem+env(safe-area-inset-bottom))] lg:pb-8"><div className="sticky top-[74px] z-20 -mx-4 mb-4 border-b border-slate-200 bg-[#f4f7f7]/95 px-4 py-3 backdrop-blur sm:-mx-7 sm:px-7"><div className="mx-auto flex max-w-6xl items-center justify-between gap-3"><div className="min-w-0"><p className="truncate text-xs font-bold uppercase tracking-[.14em] text-[#088a78]">{room.title}</p><p className="mt-1 text-xs text-slate-500">{copy(`Question ${activeIndex + 1} of ${attempt.questions.length}`, `প্রশ্ন ${activeIndex + 1} / ${attempt.questions.length}`)}</p></div><span className={`rounded-xl px-3 py-2 font-mono text-lg font-extrabold ${remaining <= 60 ? "bg-[#fff0eb] text-[#c2492c]" : "bg-[#071d33] text-white"}`}><Clock3 className="mr-1 inline" size={15} />{time}</span></div></div><div className="grid gap-5 lg:grid-cols-[1fr_250px]"><article className="rounded-[24px] bg-white p-5 shadow-sm sm:p-7"><p className="text-xs font-bold uppercase tracking-[.15em] text-slate-400">{question.subject} · {question.questionType.replaceAll("_", " ")}</p>{question.stemContext && <p className="mt-4 rounded-xl border-l-4 border-[#16b89b] bg-[#effcf9] p-3 text-sm leading-6 text-[#165e54]">{question.stemContext}</p>}<h1 className="mt-4 font-display text-xl font-extrabold leading-8 text-[#071d33]">{activeIndex + 1}. {question.prompt}</h1><div className="mt-6 grid gap-3">{question.options.map(option => <button key={option.id} onClick={() => { setSelected(current => ({ ...current, [question.id]: option.id })); save.mutate({ attemptId: attempt.attemptId, questionId: question.id, selectedOptionIds: [option.id] }, { onSuccess: () => toast.success(copy("Answer saved", "উত্তর সেভ হয়েছে"), { duration: 1200 }) }); }} className={`min-h-12 rounded-xl border p-4 text-left text-sm font-semibold ${selected[question.id] === option.id ? "border-[#16b89b] bg-[#effcf9] text-[#087b6c]" : "border-slate-200 text-slate-600"}`}><span className="mr-3 inline-flex size-6 items-center justify-center rounded-full bg-slate-100 text-xs text-slate-500">{option.optionKey}</span>{option.text}</button>)}</div></article><aside className="hidden lg:block"><div className="sticky top-[148px] rounded-[24px] bg-white p-5 shadow-sm"><p className="text-xs font-bold uppercase tracking-[.14em] text-[#088a78]">{copy("ANSWER PALETTE", "উত্তর প্যালেট")}</p><div className="mt-4 grid grid-cols-5 gap-2">{attempt.questions.map((item, index) => <button key={item.id} onClick={() => setActiveIndex(index)} className={`grid min-h-11 place-items-center rounded-lg text-xs font-bold ${activeIndex === index ? "bg-[#071d33] text-white" : selected[item.id] ? "bg-[#e9fbf7] text-[#087b6c]" : "bg-slate-100 text-slate-500"}`}>{index + 1}</button>)}</div><Button onClick={finish} disabled={submit.isPending} className="mt-5 min-h-11 w-full rounded-xl bg-[#071d33]">{copy("Finish", "শেষ করো")}</Button></div></aside></div><nav className="fixed inset-x-0 bottom-0 z-40 flex gap-3 border-t border-slate-200 bg-white/95 p-3 pb-[calc(.75rem+env(safe-area-inset-bottom))] backdrop-blur lg:hidden"><Button variant="outline" disabled={activeIndex === 0} onClick={() => setActiveIndex(index => index - 1)} className="min-h-12 flex-1 rounded-xl">{copy("Back", "পেছনে")}</Button><Button onClick={() => activeIndex === attempt.questions.length - 1 ? finish() : setActiveIndex(index => index + 1)} disabled={submit.isPending} className="min-h-12 flex-[1.4] rounded-xl bg-[#071d33]">{activeIndex === attempt.questions.length - 1 ? copy("Submit", "জমা") : copy("Next", "পরের")}</Button></nav></main>;
}

function StatusPill({ status, language }: { status: string; language: Language }) { const copy = copyFor(language); const style = status === "live" ? "bg-[#e9fbf7] text-[#087b6c]" : status === "scheduled" ? "bg-[#eef3ff] text-[#315bb3]" : "bg-slate-100 text-slate-500"; return <span className={`inline-flex rounded-full px-2.5 py-1 text-[10px] font-bold ${style}`}>{status === "live" ? copy("LIVE", "লাইভ") : status === "scheduled" ? copy("SCHEDULED", "শিডিউলড") : copy("CLOSED", "বন্ধ")}</span>; }
function Info({ icon: Icon, label, dark = false }: { icon: typeof Clock3; label: string; dark?: boolean }) { return <span className={`flex min-w-0 items-center gap-1.5 ${dark ? "text-slate-200" : "text-slate-500"}`}><Icon className={dark ? "text-[#7ce3d1]" : "text-[#088a78]"} size={14} /><span className="truncate">{label}</span></span>; }
function Leaderboard({ language, rows }: { language: Language; rows: Array<{ rank: number; name: string | null; finalScore: string | null; timeTakenSeconds: number | null; isMe: boolean }> }) { const copy = copyFor(language); return <section className="rounded-[24px] bg-white p-5 shadow-sm"><div className="flex items-center gap-2"><Trophy className="text-[#e5a523]" size={20} /><div><p className="text-sm font-extrabold text-[#071d33]">{copy("Live leaderboard", "লাইভ লিডারবোর্ড")}</p><p className="text-xs text-slate-500">{copy("Ranks update as completed results arrive.", "সম্পন্ন ফলাফল আসার সাথে র‍্যাঙ্ক আপডেট হয়।")}</p></div></div>{rows.length ? <div className="mt-4 space-y-2">{rows.map(row => <div key={row.rank} className={`flex items-center gap-3 rounded-xl p-3 text-sm ${row.isMe ? "bg-[#effcf9] text-[#087b6c]" : "bg-slate-50 text-slate-600"}`}><b className="w-6">#{row.rank}</b><span className="min-w-0 flex-1 truncate font-semibold">{row.name ?? copy("Student", "শিক্ষার্থী")}</span><span className="font-bold">{row.finalScore ?? "0"}</span><span className="text-xs">{row.timeTakenSeconds ? `${Math.ceil(row.timeTakenSeconds / 60)}m` : "—"}</span></div>)}</div> : <p className="mt-4 rounded-xl bg-slate-50 p-4 text-sm text-slate-500">{copy("Ranking appears after the first live result is submitted.", "প্রথম লাইভ ফলাফল জমা হলে র‍্যাঙ্কিং দেখা যাবে।")}</p>}</section>; }
function EmptyLiveState({ language }: { language: Language }) { const copy = copyFor(language); return <div className="grid min-h-72 place-items-center rounded-[24px] bg-white p-7 text-center shadow-sm"><div><span className="mx-auto grid size-14 place-items-center rounded-2xl bg-[#e9fbf7] text-[#088a78]"><CalendarClock /></span><h3 className="mt-4 font-display text-xl font-extrabold text-[#071d33]">{copy("No live room is scheduled yet", "এখনও কোনো লাইভ রুম শিডিউল করা হয়নি")}</h3><p className="mt-2 max-w-md text-sm leading-6 text-slate-500">{copy("When an administrator schedules a source-validated room or daily sprint, it will appear here.", "অ্যাডমিন সোর্স-ভ্যালিডেটেড রুম বা ডেইলি স্প্রিন্ট শিডিউল করলে এখানে দেখা যাবে।")}</p></div></div>; }
function RoomSkeletons() { return <div className="grid gap-4 lg:grid-cols-2">{[1, 2].map(item => <div key={item} className="h-64 animate-pulse rounded-[24px] bg-white" />)}</div>; }
function LiveRoomSkeleton() { return <div className="mx-auto max-w-5xl space-y-5"><div className="h-8 w-40 animate-pulse rounded bg-slate-200" /><div className="h-64 animate-pulse rounded-[28px] bg-white" /></div>; }
function RecoveryCard({ message, onRetry, language }: { message: string; onRetry: () => void; language: Language }) { const copy = copyFor(language); return <div className="grid min-h-60 place-items-center rounded-[24px] bg-white p-7 text-center shadow-sm"><div><AlertTriangle className="mx-auto text-[#b86b09]" /><h2 className="mt-3 font-display text-xl font-extrabold text-[#071d33]">{copy("Live room could not load", "লাইভ রুম লোড করা যায়নি")}</h2><p className="mt-2 text-sm text-slate-500">{message}</p><Button onClick={onRetry} className="mt-5 rounded-xl bg-[#071d33]">{copy("Try again", "আবার চেষ্টা করো")}</Button></div></div>; }
