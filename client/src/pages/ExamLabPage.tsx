import { MathFormula } from "@/components/MathFormula";
import { PlatformShell, type Language } from "@/components/PlatformShell";
import { Button } from "@/components/ui/button";
import { scoreMcqExam } from "@shared/mcq";
import { CheckCircle2, Clock3, Flag, ListChecks, Play, RotateCcw, ShieldCheck, Timer, Trophy } from "lucide-react";
import { useEffect, useMemo, useState } from "react";

type LabQuestion = { id: number; subject: string; chapter: string; type: "single_mcq" | "multi_statement" | "stem_subquestion"; prompt: React.ReactNode; options: string[]; correct: number; marks: number; negative: number; explanation: React.ReactNode; context?: string };

const labQuestions: LabQuestion[] = [
  { id: 101, subject: "Mathematics", chapter: "Quadratic equation", type: "single_mcq", prompt: <>For <MathFormula latex="x^2 - 5x + 6 = 0" />, what is the sum of roots?</>, options: ["3", "5", "6", "−5"], correct: 1, marks: 1, negative: 0.25, explanation: <>For <MathFormula latex="ax^2+bx+c=0" />, the sum is <MathFormula latex="-\frac{b}{a}" />. Here it equals <MathFormula latex="5" />.</> },
  { id: 102, subject: "Physics", chapter: "Motion", type: "multi_statement", prompt: <>Which combination is correct for a scalar quantity? Statements: i. has magnitude; ii. has direction; iii. follows ordinary algebra.</>, options: ["i & ii", "ii & iii", "i & iii", "i, ii & iii"], correct: 2, marks: 1, negative: 0.25, explanation: "A scalar has magnitude and follows ordinary algebra, but does not have direction." },
  { id: 103, subject: "Chemistry", chapter: "Chemical bonding", type: "stem_subquestion", context: "A sodium atom transfers one electron to a chlorine atom while forming a stable compound.", prompt: "Which statement best describes the bond in the compound?", options: ["Covalent bond", "Ionic bond", "Metallic bond", "Hydrogen bond"], correct: 1, marks: 1, negative: 0.25, explanation: "Electron transfer between atoms forms an ionic bond." },
];

export default function ExamLabPage({ language, onLanguageChange }: { language: Language; onLanguageChange: (value: Language) => void }) {
  const copy = (en: string, bn: string) => language === "bn" ? bn : en;
  const [duration, setDuration] = useState(10);
  const [started, setStarted] = useState(false);
  const [remaining, setRemaining] = useState(0);
  const [current, setCurrent] = useState(0);
  const [answers, setAnswers] = useState<Record<number, number>>({});
  const [review, setReview] = useState<number[]>([]);
  const [finished, setFinished] = useState(false);
  const question = labQuestions[current]!;
  const result = useMemo(() => scoreMcqExam(labQuestions.map(item => ({ id: item.id, correctOptionIds: [item.correct], marks: item.marks, negativeMarkWeight: item.negative, subject: item.subject, chapter: item.chapter })), Object.entries(answers).map(([id, selected]) => ({ questionId: Number(id), selectedOptionIds: [selected] }))), [answers]);
  useEffect(() => {
    if (!started || finished) return;
    const interval = window.setInterval(() => setRemaining(value => {
      if (value <= 1) { window.clearInterval(interval); setFinished(true); return 0; }
      return value - 1;
    }), 1000);
    return () => window.clearInterval(interval);
  }, [started, finished]);
  const begin = () => { setStarted(true); setFinished(false); setRemaining(duration * 60); setCurrent(0); setAnswers({}); setReview([]); };
  const reset = () => { setStarted(false); setFinished(false); setAnswers({}); setReview([]); };
  const time = `${String(Math.floor(remaining / 60)).padStart(2, "0")}:${String(remaining % 60).padStart(2, "0")}`;

  if (!started) return <PlatformShell language={language} onLanguageChange={onLanguageChange}><div className="mx-auto max-w-5xl"><div className="grid gap-6 lg:grid-cols-[.76fr_1.24fr]"><aside className="rounded-[28px] bg-[#071d33] p-7 text-white"><span className="grid size-12 place-items-center rounded-2xl bg-[#16b89b] text-[#071d33]"><Trophy /></span><p className="mt-6 text-xs font-bold uppercase tracking-[.16em] text-[#7ce3d1]">MCQ GURU EXAM LAB</p><h1 className="mt-2 font-display text-3xl font-extrabold">{copy("Practice under calm, real-exam conditions.", "শান্ত বাস্তব-পরীক্ষার পরিবেশে প্র্যাকটিস করো।")}</h1><p className="mt-4 text-sm leading-6 text-slate-300">{copy("This offline-capable practice run uses a visible marking policy, question palette, review flags, automatic timeout submission, and a clear explanation breakdown.", "এই অফলাইন-সক্ষম প্র্যাকটিস রানে দৃশ্যমান মার্কিং পলিসি, প্রশ্ন তালিকা, রিভিউ ফ্ল্যাগ, অটো-সাবমিশন ও ব্যাখ্যা আছে।")}</p><div className="mt-7 rounded-2xl border border-[#7ce3d1]/20 bg-[#16b89b]/10 p-4 text-xs leading-5 text-slate-200"><ShieldCheck className="mr-1 inline text-[#7ce3d1]" size={14} />{copy("Negative marking is set explicitly per question. This demo applies −0.25 to a wrong answer.", "নেগেটিভ মার্কিং প্রতি প্রশ্নে স্পষ্টভাবে নির্ধারিত। এই ডেমোতে ভুল উত্তরে −০.২৫ প্রযোজ্য।")}</div></aside><section className="rounded-[28px] bg-white p-6 shadow-sm sm:p-8"><p className="text-xs font-bold uppercase tracking-[.16em] text-[#088a78]">{copy("CONFIGURE YOUR RUN", "রান কনফিগার করো")}</p><h2 className="mt-2 font-display text-2xl font-extrabold text-[#071d33]">{copy("HSC concept sprint", "এইচএসসি কনসেপ্ট স্প্রিন্ট")}</h2><p className="mt-2 text-sm text-slate-500">{copy("3 mixed question types · 3 marks · supports LaTex and Bengali rendering", "৩টি মিক্সড প্রশ্ন টাইপ · ৩ মার্ক · ল্যাটেক্স ও বাংলা রেন্ডারিং সাপোর্ট")}</p><div className="mt-8"><p className="text-sm font-bold text-slate-700">{copy("Duration", "সময়")}</p><div className="mt-3 grid grid-cols-3 gap-2">{[5, 10, 20].map(value => <button key={value} onClick={() => setDuration(value)} className={`rounded-xl py-3 text-sm font-bold ${duration === value ? "bg-[#071d33] text-white" : "bg-slate-100 text-slate-500"}`}>{value} {copy("min", "মিনিট")}</button>)}</div></div><div className="mt-8 grid grid-cols-3 gap-3"><LabStat icon={ListChecks} label={copy("Questions", "প্রশ্ন")} value="3" /><LabStat icon={CheckCircle2} label={copy("Total marks", "মোট মার্ক")} value="3" /><LabStat icon={Timer} label={copy("Wrong", "ভুল")} value="−0.25" /></div><Button onClick={begin} className="mt-8 h-12 w-full rounded-xl bg-[#071d33]"><Play size={16} fill="currentColor" />{copy("Start timed practice", "টাইমড প্র্যাকটিস শুরু করো")}</Button></section></div></div></PlatformShell>;
  if (finished) return <PlatformShell language={language} onLanguageChange={onLanguageChange}><div className="mx-auto max-w-5xl"><div className="rounded-[28px] bg-[#071d33] p-7 text-white sm:p-9"><p className="text-xs font-bold uppercase tracking-[.16em] text-[#7ce3d1]">{copy("SESSION ANALYSIS", "সেশন অ্যানালাইসিস")}</p><h1 className="mt-2 font-display text-3xl font-extrabold">{copy("Your score, with the why behind it.", "তোমার স্কোর, এবং এর কারণ।")}</h1><div className="mt-7 grid gap-4 sm:grid-cols-4"><ResultStat value={String(result.netMarks)} label={copy("Net marks", "নেট মার্ক")} /><ResultStat value={`${result.accuracy}%`} label={copy("Accuracy", "নির্ভুলতা")} /><ResultStat value={String(result.correct)} label={copy("Correct", "সঠিক")} /><ResultStat value={`−${result.negativeMarks}`} label={copy("Negative", "নেগেটিভ")} /></div></div><div className="mt-6 grid gap-6 lg:grid-cols-[.8fr_1.2fr]"><section className="rounded-[24px] bg-white p-6 shadow-sm"><h2 className="font-display text-xl font-extrabold text-[#071d33]">{copy("Weakest chapter", "সবচেয়ে দুর্বল অধ্যায়")}</h2><p className="mt-2 text-sm text-slate-500">{result.chapterAccuracy[0]?.chapter ?? copy("No attempts", "কোনো চেষ্টা নেই")}</p><div className="mt-5 space-y-3">{result.chapterAccuracy.map(item => <div key={item.chapter}><div className="flex justify-between text-xs"><span className="font-semibold text-slate-600">{item.subject} · {item.chapter}</span><b className="text-[#088a78]">{item.accuracy}%</b></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-[#16b89b]" style={{ width: `${item.accuracy}%` }} /></div></div>)}</div><Button onClick={reset} variant="outline" className="mt-7 h-10 rounded-xl"><RotateCcw size={15} />{copy("Try again", "আবার চেষ্টা")}</Button></section><section className="space-y-4">{labQuestions.map((item, index) => <article key={item.id} className="rounded-[20px] bg-white p-5 shadow-sm"><div className="flex justify-between gap-3"><span className="text-xs font-bold text-[#088a78]">{copy(`QUESTION ${index + 1}`, `প্রশ্ন ${index + 1}`)}</span><span className={answers[item.id] === item.correct ? "text-xs font-bold text-[#088a78]" : "text-xs font-bold text-[#c2492c]"}>{answers[item.id] === item.correct ? copy("Correct", "সঠিক") : copy("Review", "রিভিউ")}</span></div><div className="mt-3 text-sm font-bold leading-6 text-[#071d33]">{item.prompt}</div><p className="mt-3 rounded-xl bg-[#effcf9] p-3 text-sm leading-6 text-[#165e54]">{item.explanation}</p></article>)}</section></div></div></PlatformShell>;
  return <PlatformShell language={language} onLanguageChange={onLanguageChange}><div className="mx-auto max-w-6xl"><div className="mb-5 flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-[#088a78]">HSC CONCEPT SPRINT</p><p className="mt-1 text-sm text-slate-500">{copy("Autosaves in this browser while offline.", "অফলাইনে এই ব্রাউজারে অটোসেভ হয়।")}</p></div><span className="rounded-xl bg-[#071d33] px-3 py-2 font-mono text-lg font-bold text-white"><Clock3 className="mr-2 inline text-[#7ce3d1]" size={16} />{time}</span></div><div className="grid gap-6 lg:grid-cols-[1fr_245px]"><section className="rounded-[24px] bg-white p-6 shadow-sm sm:p-8">{question.context && <div className="mb-6 rounded-2xl border-l-4 border-[#16b89b] bg-[#effcf9] p-4 text-sm leading-6 text-[#165e54]"><b>{copy("Shared stem", "শেয়ার্ড স্টেম")}:</b> {question.context}</div>}<p className="text-xs font-bold uppercase tracking-[.16em] text-slate-400">{copy(`QUESTION ${current + 1} OF ${labQuestions.length}`, `প্রশ্ন ${current + 1} / ${labQuestions.length}`)} · {question.type.replaceAll("_", " ")}</p><h1 className="mt-4 font-display text-xl font-extrabold leading-8 text-[#071d33]">{question.prompt}</h1><div className="mt-7 grid gap-3">{question.options.map((option, index) => <button key={option} onClick={() => setAnswers({ ...answers, [question.id]: index })} className={`flex items-center gap-3 rounded-xl border p-4 text-left text-sm font-semibold ${answers[question.id] === index ? "border-[#16b89b] bg-[#effcf9] text-[#087b6c]" : "border-slate-200 text-slate-600 hover:border-[#9fded3]"}`}><span className={`grid size-7 place-items-center rounded-full border text-xs ${answers[question.id] === index ? "border-[#16b89b] bg-[#16b89b] text-[#071d33]" : "border-slate-300"}`}>{String.fromCharCode(65 + index)}</span>{option}</button>)}</div><div className="mt-8 flex flex-wrap items-center justify-between gap-3 border-t border-slate-100 pt-5"><button onClick={() => setReview(review.includes(question.id) ? review.filter(id => id !== question.id) : [...review, question.id])} className={`flex items-center gap-2 text-sm font-bold ${review.includes(question.id) ? "text-[#b86b09]" : "text-slate-500"}`}><Flag size={16} fill={review.includes(question.id) ? "currentColor" : "none"} />{review.includes(question.id) ? copy("Marked for review", "রিভিউর জন্য রাখা") : copy("Mark for review", "রিভিউর জন্য রাখো")}</button><div className="flex gap-2"><Button variant="outline" disabled={current === 0} onClick={() => setCurrent(current - 1)} className="rounded-xl">{copy("Previous", "আগের")}</Button><Button onClick={() => current === labQuestions.length - 1 ? setFinished(true) : setCurrent(current + 1)} className="rounded-xl bg-[#071d33]">{current === labQuestions.length - 1 ? copy("Submit", "সাবমিট") : copy("Next", "পরের")}</Button></div></div></section><aside className="rounded-[24px] bg-white p-5 shadow-sm"><p className="text-xs font-bold uppercase tracking-[.16em] text-slate-400">{copy("QUESTION PALETTE", "প্রশ্ন তালিকা")}</p><div className="mt-4 grid grid-cols-3 gap-2">{labQuestions.map((item, index) => <button key={item.id} onClick={() => setCurrent(index)} className={`aspect-square rounded-lg text-xs font-bold ${index === current ? "bg-[#16b89b] text-[#071d33]" : answers[item.id] !== undefined ? "bg-[#e9fbf7] text-[#088a78]" : review.includes(item.id) ? "border-2 border-[#f59e0b] bg-[#fff7e4] text-[#b86b09]" : "bg-slate-100 text-slate-500"}`}>{index + 1}</button>)}</div><div className="mt-6 space-y-2 text-xs text-slate-500"><p><span className="mr-2 inline-block size-2 rounded-sm bg-[#16b89b]" />{copy("Current", "বর্তমান")}</p><p><span className="mr-2 inline-block size-2 rounded-sm bg-[#e9fbf7]" />{copy("Answered", "উত্তর দেওয়া")}</p><p><span className="mr-2 inline-block size-2 rounded-sm border-2 border-[#f59e0b]" />{copy("Review", "রিভিউ")}</p></div></aside></div></div></PlatformShell>;
}
function LabStat({ icon: Icon, label, value }: { icon: typeof ListChecks; label: string; value: string }) { return <div className="rounded-xl bg-[#f8fbfb] p-3"><Icon size={16} className="text-[#088a78]" /><p className="mt-2 text-lg font-extrabold text-[#071d33]">{value}</p><p className="text-[11px] text-slate-500">{label}</p></div>; }
function ResultStat({ value, label }: { value: string; label: string }) { return <div className="rounded-2xl border border-white/10 bg-white/5 p-4"><p className="text-2xl font-extrabold text-[#7ce3d1]">{value}</p><p className="mt-1 text-xs text-slate-300">{label}</p></div>; }
