import { PlatformShell, type Language } from "@/components/PlatformShell";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { FileClock, Landmark, Plus, ShieldCheck } from "lucide-react";
import { useState } from "react";
import { toast } from "sonner";

export default function AdmissionPatternsPage({ language, onLanguageChange }: { language: Language; onLanguageChange: (value: Language) => void }) {
  const copy = (en: string, bn: string) => language === "bn" ? bn : en;
  const patterns = trpc.learning.admissionPatternVersions.useQuery();
  const utils = trpc.useUtils();
  const [form, setForm] = useState({ institution: "University of Dhaka", title: "Undergraduate Admission", unit: "", versionLabel: "2025–26 source review", sourceUrl: "https://admission.eis.du.ac.bd/en/408b7c8ad06e4d9954fa2d948a01f508", notes: "", status: "under_review" as const });
  const create = trpc.learning.createAdmissionPatternVersion.useMutation({ onSuccess: () => { toast.success(copy("Admission pattern version saved for review", "ভর্তি প্যাটার্ন ভার্সন রিভিউয়ের জন্য সংরক্ষণ হয়েছে")); utils.learning.admissionPatternVersions.invalidate(); }, onError: error => toast.error(error.message) });
  return <PlatformShell language={language} onLanguageChange={onLanguageChange}><div className="mx-auto max-w-6xl"><div className="grid gap-6 xl:grid-cols-[.85fr_1.15fr]"><aside className="rounded-[24px] bg-[#071d33] p-6 text-white"><span className="grid size-11 place-items-center rounded-2xl bg-[#16b89b] text-[#071d33]"><Landmark /></span><p className="mt-5 text-xs font-bold uppercase tracking-[.16em] text-[#7ce3d1]">{copy("VERSIONED ADMISSION PATTERNS", "ভার্সনড ভর্তি প্যাটার্ন")}</p><h1 className="mt-2 font-display text-2xl font-extrabold">{copy("Keep each admission rule tied to an official source.", "প্রতিটি ভর্তি নিয়মকে অফিসিয়াল সোর্সের সাথে যুক্ত রাখো।")}</h1><p className="mt-4 text-sm leading-6 text-slate-300">{copy("Unknown values stay blank until the source is reviewed. A version can be active only after a reviewer confirms the official notice.", "অজানা ভ্যালু সোর্স রিভিউ না হওয়া পর্যন্ত ফাঁকা থাকবে। অফিসিয়াল নোটিশ রিভিউয়ার নিশ্চিত করলেই শুধু ভার্সন অ্যাক্টিভ হতে পারবে।")}</p><div className="mt-8 rounded-2xl border border-[#7ce3d1]/20 bg-[#16b89b]/10 p-4 text-xs leading-5 text-slate-200"><ShieldCheck className="mr-1 inline text-[#7ce3d1]" size={14} />{copy("No deadlines, marks, or eligibility facts are inferred.", "কোনো ডেডলাইন, মার্ক বা যোগ্যতা অনুমান করা হয় না।")}</div></aside><section className="rounded-[24px] bg-white p-5 shadow-sm sm:p-7"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-[#eef3ff] text-[#315bb3]"><FileClock size={18} /></span><div><h2 className="font-display text-xl font-extrabold text-[#071d33]">{copy("Pattern versions", "প্যাটার্ন ভার্সন")}</h2><p className="text-xs text-slate-500">{copy("Institution-level source traceability", "প্রতিষ্ঠানভিত্তিক সোর্স ট্রেসেবিলিটি")}</p></div></div><div className="mt-5 space-y-3">{patterns.data?.length ? patterns.data.map(pattern => <article key={pattern.id} className="rounded-xl border border-slate-100 p-4"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-bold text-[#088a78]">{pattern.institution}</p><h3 className="mt-1 text-sm font-extrabold text-[#071d33]">{pattern.title}</h3><p className="mt-1 text-xs text-slate-500">{pattern.versionLabel} · {pattern.status}</p></div><span className="rounded-md bg-slate-100 px-2 py-1 text-[10px] font-bold text-slate-500">v{pattern.id}</span></div></article>) : <p className="rounded-xl bg-slate-50 p-4 text-sm text-slate-500">{copy("No pattern versions have been recorded yet.", "এখনো কোনো প্যাটার্ন ভার্সন রেকর্ড করা হয়নি।")}</p>}</div><div className="mt-6 grid gap-3 sm:grid-cols-2"><input value={form.institution} onChange={event => setForm({ ...form, institution: event.target.value })} className="h-10 rounded-xl border border-slate-200 px-3 text-sm" placeholder={copy("Institution", "প্রতিষ্ঠান")} /><input value={form.title} onChange={event => setForm({ ...form, title: event.target.value })} className="h-10 rounded-xl border border-slate-200 px-3 text-sm" placeholder={copy("Exam title", "এক্সাম শিরোনাম")} /><input value={form.versionLabel} onChange={event => setForm({ ...form, versionLabel: event.target.value })} className="h-10 rounded-xl border border-slate-200 px-3 text-sm" placeholder={copy("Version label", "ভার্সন লেবেল")} /><input value={form.sourceUrl} onChange={event => setForm({ ...form, sourceUrl: event.target.value })} className="h-10 rounded-xl border border-slate-200 px-3 text-sm" placeholder={copy("Official source URL", "অফিসিয়াল সোর্স ইউআরএল")} /><textarea value={form.notes} onChange={event => setForm({ ...form, notes: event.target.value })} className="min-h-20 rounded-xl border border-slate-200 p-3 text-sm sm:col-span-2" placeholder={copy("Verified notes only (optional)", "শুধু যাচাইকৃত নোট (ঐচ্ছিক)")} /></div><Button disabled={create.isPending} onClick={() => create.mutate(form)} className="mt-4 h-10 w-full rounded-xl bg-[#071d33]"><Plus size={15} />{copy("Save for source review", "সোর্স রিভিউয়ের জন্য সেভ")}</Button></section></div></div></PlatformShell>;
}
