import { PlatformShell, type Language } from "@/components/PlatformShell";
import { trpc } from "@/lib/trpc";
import { ArrowUpRight, Bell, CalendarDays, FileCheck2, Landmark, ShieldCheck } from "lucide-react";

export default function OfficialNoticesPage({ language, onLanguageChange }: { language: Language; onLanguageChange: (value: Language) => void }) {
  const copy = (en: string, bn: string) => language === "bn" ? bn : en;
  const notices = trpc.learning.publishedAdmissionNotices.useQuery();
  const date = (value: Date | string | null) => value ? new Intl.DateTimeFormat(language === "bn" ? "bn-BD" : "en-GB", { dateStyle: "medium" }).format(new Date(value)) : copy("Not dated", "তারিখ নেই");
  return <PlatformShell language={language} onLanguageChange={onLanguageChange}><div className="mx-auto max-w-5xl"><div className="grid gap-5 lg:grid-cols-[1fr_auto]"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-[#088a78]">{copy("OFFICIAL ADMISSION UPDATES", "অফিসিয়াল ভর্তি আপডেট")}</p><h1 className="mt-1 font-display text-2xl font-extrabold text-[#071d33]">{copy("Notices linked to their original source", "মূল উৎসের সাথে যুক্ত নোটিশ")}</h1><p className="mt-3 max-w-2xl text-sm leading-6 text-slate-500">{copy("Every published update includes an official source link and a captured retrieval date. We do not infer deadlines or eligibility from incomplete notices.", "প্রতিটি প্রকাশিত আপডেটে অফিসিয়াল সোর্স লিংক ও সংগ্রহের তারিখ থাকে। অসম্পূর্ণ নোটিশ থেকে আমরা ডেডলাইন বা যোগ্যতা অনুমান করি না।")}</p></div><div className="rounded-2xl bg-[#e9fbf7] px-4 py-3 text-sm font-bold text-[#087b6c]"><ShieldCheck className="mr-2 inline" size={16} />{copy("Source-first registry", "সোর্স-ফার্স্ট রেজিস্ট্রি")}</div></div>{notices.isLoading ? <div className="mt-7 grid gap-4 md:grid-cols-2">{[1, 2].map(item => <div key={item} className="h-52 animate-pulse rounded-2xl bg-white" />)}</div> : notices.data?.length ? <div className="mt-7 grid gap-4 md:grid-cols-2">{notices.data.map(notice => <article key={notice.id} className="rounded-[24px] bg-white p-5 shadow-sm"><div className="flex items-start justify-between gap-3"><span className="grid size-10 place-items-center rounded-xl bg-[#eef3ff] text-[#315bb3]"><Landmark size={18} /></span><span className="rounded-md bg-[#e9fbf7] px-2 py-1 text-[10px] font-extrabold uppercase text-[#087b6c]">{notice.noticeType}</span></div><p className="mt-4 text-xs font-bold text-[#088a78]">{notice.institution} · {notice.session}</p><h2 className="mt-2 font-display text-lg font-extrabold leading-7 text-[#071d33]">{notice.title}</h2>{notice.summary && <p className="mt-3 line-clamp-3 text-sm leading-6 text-slate-500">{notice.summary}</p>}<div className="mt-5 flex items-center justify-between border-t border-slate-100 pt-4"><span className="flex items-center gap-1 text-xs text-slate-400"><CalendarDays size={13} />{date(notice.retrievedAt)}</span><a href={notice.sourceUrl} target="_blank" rel="noreferrer" className="text-xs font-bold text-[#088a78]">{copy("Open official notice", "অফিসিয়াল নোটিশ") }<ArrowUpRight className="ml-1 inline" size={14} /></a></div></article>)}</div> : <div className="mt-7 grid min-h-80 place-items-center rounded-[28px] bg-white p-8 text-center shadow-sm"><div><span className="mx-auto grid size-12 place-items-center rounded-2xl bg-[#e9fbf7] text-[#088a78]"><FileCheck2 /></span><h2 className="mt-4 font-display text-xl font-extrabold text-[#071d33]">{copy("No verified notices published yet", "এখনো কোনো যাচাইকৃত নোটিশ প্রকাশ হয়নি")}</h2><p className="mt-2 max-w-md text-sm leading-6 text-slate-500">{copy("Official notices will appear here after a content reviewer confirms the source and publishes the captured record.", "কনটেন্ট রিভিউয়ার সোর্স যাচাই ও রেকর্ড প্রকাশ করার পর অফিসিয়াল নোটিশ এখানে আসবে।")}</p></div></div>}</div></PlatformShell>;
}
