import { PlatformShell, type Language } from "@/components/PlatformShell";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { BellRing, BookOpenCheck, ExternalLink, FileText, Landmark, Plus, ShieldCheck } from "lucide-react";
import { useState } from "react";
import { Link } from "wouter";
import { toast } from "sonner";

export default function GovernanceHub({ language, onLanguageChange }: { language: Language; onLanguageChange: (value: Language) => void }) {
  const copy = (en: string, bn: string) => language === "bn" ? bn : en;
  const sources = trpc.learning.approvedSources.useQuery();
  const utils = trpc.useUtils();
  const [source, setSource] = useState({ organization: "National Curriculum and Textbook Board (NCTB)", title: "2025–26 Higher Secondary (Class XI–XII) Textbook Listing", sourceUrl: "https://nctb.gov.bd/pages/static-pages/6922e145933eb65569e2b37b", sourceType: "nctb" as const, versionLabel: "Observed 2026-08-16", contentHash: "nctb-hsc-listing-2026-08-16", status: "active" as const });
  const [notice, setNotice] = useState({ sourceId: "", institution: "University of Dhaka", title: "Undergraduate Admission System", session: "2025–26", noticeType: "application" as const, sourceUrl: "https://admission.eis.du.ac.bd/en/408b7c8ad06e4d9954fa2d948a01f508", summary: "", status: "under_review" as const });
  const [notification, setNotification] = useState<{ userId: string; title: string; body: string; type: "study" | "admission" | "content" | "account" | "system"; priority: "normal" | "high" | "critical"; actionUrl: string }>({ userId: "", title: "", body: "", type: "system", priority: "normal", actionUrl: "/notifications" });
  const registerSource = trpc.learning.registerOfficialSource.useMutation({ onSuccess: () => { toast.success(copy("Official source registered", "অফিসিয়াল সোর্স রেজিস্টার হয়েছে")); utils.learning.approvedSources.invalidate(); } });
  const createNotice = trpc.learning.createAdmissionNotice.useMutation({ onSuccess: () => toast.success(copy("Notice record created", "নোটিশ রেকর্ড তৈরি হয়েছে")) });
  const sendNotification = trpc.learning.sendCustomNotification.useMutation({ onSuccess: data => toast.success(data.ownerNotified ? copy("Alert sent and owner escalated", "অ্যালার্ট পাঠানো ও ওনারকে জানানো হয়েছে") : data.delivered ? copy("Custom notification sent", "কাস্টম নোটিফিকেশন পাঠানো হয়েছে") : copy("Notification respected the student’s category preference", "শিক্ষার্থীর ক্যাটাগরি পছন্দ অনুযায়ী নোটিফিকেশন পাঠানো হয়নি")) });
  const error = (mutation: { error?: { message: string } | null }) => mutation.error?.message && <p className="mt-2 text-xs font-semibold text-[#c2492c]">{mutation.error.message}</p>;
  return <PlatformShell language={language} onLanguageChange={onLanguageChange}><div className="mx-auto max-w-6xl"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs font-bold uppercase tracking-[.16em] text-[#088a78]">{copy("CONTENT GOVERNANCE", "কনটেন্ট গভর্ন্যান্স")}</p><h1 className="mt-1 font-display text-2xl font-extrabold text-[#071d33]">{copy("Sources, notices, and student alerts", "সোর্স, নোটিশ এবং শিক্ষার্থী অ্যালার্ট")}</h1><p className="mt-2 max-w-3xl text-sm leading-6 text-slate-500">{copy("Register canonical official links, capture a versioned notice record, and deliver purposeful student notifications. Source content is not copied into the app without explicit rights.", "ক্যানোনিক্যাল অফিসিয়াল লিংক রেজিস্টার করো, ভার্সনড নোটিশ রেকর্ড রাখো এবং প্রয়োজনীয় শিক্ষার্থী নোটিফিকেশন পাঠাও। স্পষ্ট অধিকার ছাড়া সোর্স কনটেন্ট অ্যাপে কপি করা হয় না।")}</p></div><Link href="/admin" className="inline-flex items-center gap-2 rounded-xl bg-[#071d33] px-4 py-2.5 text-sm font-bold text-white"><BookOpenCheck size={16} />{copy("Question review", "প্রশ্ন রিভিউ")}</Link></div><div className="mt-6 grid gap-6 xl:grid-cols-2"><section className="rounded-[24px] bg-white p-5 shadow-sm sm:p-6"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-[#e9fbf7] text-[#088a78]"><BookOpenCheck size={18} /></span><div><h2 className="font-display text-lg font-extrabold text-[#071d33]">{copy("Approved-source registry", "অনুমোদিত সোর্স রেজিস্ট্রি")}</h2><p className="text-xs text-slate-500">{copy("Metadata and version verification", "মেটাডেটা ও ভার্সন যাচাই")}</p></div></div><div className="mt-5 space-y-3">{sources.data?.length ? sources.data.map(item => <a key={`${item.id}-${item.versionLabel ?? ""}`} href={item.sourceUrl} target="_blank" rel="noreferrer" className="flex items-center gap-3 rounded-xl border border-slate-100 p-3 hover:border-[#bdeadd]"><span className="grid size-8 place-items-center rounded-lg bg-[#f1f8ff] text-[#315bb3]"><FileText size={15} /></span><span className="min-w-0 flex-1"><b className="block truncate text-xs text-[#071d33]">{item.title}</b><span className="block truncate text-[11px] text-slate-500">{item.organization} · {item.versionLabel ?? copy("No version yet", "কোনো ভার্সন নেই")}</span></span><ExternalLink className="text-slate-400" size={14} /></a>) : <p className="rounded-xl bg-slate-50 p-4 text-sm text-slate-500">{copy("No source has been registered yet.", "এখনো কোনো সোর্স রেজিস্টার হয়নি।")}</p>}</div><div className="mt-5 grid gap-3"><input value={source.title} onChange={event => setSource({ ...source, title: event.target.value })} className="h-10 rounded-xl border border-slate-200 px-3 text-sm" /><input value={source.sourceUrl} onChange={event => setSource({ ...source, sourceUrl: event.target.value })} className="h-10 rounded-xl border border-slate-200 px-3 text-sm" /><Button disabled={registerSource.isPending} onClick={() => registerSource.mutate(source)} className="h-10 rounded-xl bg-[#071d33]"><Plus size={15} />{copy("Register official NCTB listing", "অফিসিয়াল এনসিটিবি লিস্টিং রেজিস্টার")}</Button>{error(registerSource)}</div></section><section className="rounded-[24px] bg-white p-5 shadow-sm sm:p-6"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-[#fff5e4] text-[#b86b09]"><Landmark size={18} /></span><div><h2 className="font-display text-lg font-extrabold text-[#071d33]">{copy("Official admission notice", "অফিসিয়াল ভর্তি নোটিশ")}</h2><p className="text-xs text-slate-500">{copy("Source link and review state required", "সোর্স লিংক ও রিভিউ স্টেট প্রয়োজন")}</p></div></div><div className="mt-5 grid gap-3"><input value={notice.sourceId} onChange={event => setNotice({ ...notice, sourceId: event.target.value })} className="h-10 rounded-xl border border-slate-200 px-3 text-sm" placeholder={copy("Registered source ID", "রেজিস্টার্ড সোর্স আইডি")} /><input value={notice.institution} onChange={event => setNotice({ ...notice, institution: event.target.value })} className="h-10 rounded-xl border border-slate-200 px-3 text-sm" /><input value={notice.title} onChange={event => setNotice({ ...notice, title: event.target.value })} className="h-10 rounded-xl border border-slate-200 px-3 text-sm" /><input value={notice.sourceUrl} onChange={event => setNotice({ ...notice, sourceUrl: event.target.value })} className="h-10 rounded-xl border border-slate-200 px-3 text-sm" /><textarea value={notice.summary} onChange={event => setNotice({ ...notice, summary: event.target.value })} className="min-h-20 rounded-xl border border-slate-200 p-3 text-sm" placeholder={copy("Verified summary only (optional)", "শুধু যাচাইকৃত সারাংশ (ঐচ্ছিক)")} /><Button disabled={createNotice.isPending || !notice.sourceId} onClick={() => createNotice.mutate({ ...notice, sourceId: Number(notice.sourceId) })} className="h-10 rounded-xl bg-[#071d33]"><FileText size={15} />{copy("Create review record", "রিভিউ রেকর্ড তৈরি")}</Button>{error(createNotice)}</div></section><section className="rounded-[24px] bg-[#071d33] p-5 text-white shadow-sm sm:p-6 xl:col-span-2"><div className="flex items-center gap-3"><span className="grid size-10 place-items-center rounded-xl bg-[#16b89b] text-[#071d33]"><BellRing size={18} /></span><div><h2 className="font-display text-lg font-extrabold">{copy("Custom student notification", "কাস্টম শিক্ষার্থী নোটিফিকেশন")}</h2><p className="text-xs text-slate-300">{copy("A critical alert also escalates to the project owner.", "ক্রিটিক্যাল অ্যালার্ট প্রজেক্ট ওনারের কাছেও যায়।")}</p></div></div><div className="mt-5 grid gap-3 md:grid-cols-2"><input value={notification.userId} onChange={event => setNotification({ ...notification, userId: event.target.value })} className="h-10 rounded-xl border border-white/10 bg-white/8 px-3 text-sm outline-none" placeholder={copy("Student user ID", "শিক্ষার্থীর ইউজার আইডি")} /><input value={notification.title} onChange={event => setNotification({ ...notification, title: event.target.value })} className="h-10 rounded-xl border border-white/10 bg-white/8 px-3 text-sm outline-none" placeholder={copy("Notification title", "নোটিফিকেশনের শিরোনাম")} /><textarea value={notification.body} onChange={event => setNotification({ ...notification, body: event.target.value })} className="min-h-20 rounded-xl border border-white/10 bg-white/8 p-3 text-sm outline-none md:col-span-2" placeholder={copy("A clear, respectful message", "স্পষ্ট ও সম্মানজনক মেসেজ")} /><select value={notification.priority} onChange={event => setNotification({ ...notification, priority: event.target.value as "normal" | "high" | "critical" })} className="h-10 rounded-xl border border-white/10 bg-[#0c2a45] px-3 text-sm"><option value="normal">Normal</option><option value="high">High</option><option value="critical">Critical</option></select><Button disabled={sendNotification.isPending || !notification.userId || !notification.title || !notification.body} onClick={() => sendNotification.mutate({ ...notification, userId: Number(notification.userId) })} className="h-10 rounded-xl bg-[#16b89b] font-bold text-[#071d33] hover:bg-[#24c8aa]"><BellRing size={15} />{copy("Send custom notification", "কাস্টম নোটিফিকেশন পাঠাও")}</Button></div>{error(sendNotification)}</section></div></div></PlatformShell>;
}
